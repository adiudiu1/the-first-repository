---
name: dynamic-job-shop-rl-research
description: Use when exploring reinforcement-learning methods for dynamic job shop scheduling with stochastic job arrivals and a disjunctive-graph representation, especially when framing the problem, auditing unresolved choices, planning early experiments, or routing work to research skills.
---

# Dynamic Job Shop RL Research

## Confirmed scope

The user is conducting early exploratory research on reinforcement-learning methods for the dynamic job shop scheduling problem with stochastic job arrivals and a disjunctive-graph representation.

This statement is the only built-in research fact. All mathematical, algorithmic, simulator, and experimental details must come from the current request or inspected files.

## Start here

Read references/research-checklist.md. Classify every item as confirmed with a source, unresolved, or conflict. Never fill unresolved items from convention. Ask one high-impact question at a time or create a labeled open-question list.

## Workflow

1. Inspect instructions, documentation, configurations, simulator, environment, algorithms, tests, and result schemas in scope.
2. Map the problem, arrivals, graph, MDP, simulator, learning method, baselines, measures, protocol, and resources.
3. Identify the earliest uncertainty blocking a valid experiment.
4. Route work to experiment-design, experiment-code, data-analysis, or research-context-compressor.
5. After each round record what changed, supporting evidence, and the next decision.

## Early-stage priorities

Prefer simulator and feasibility invariants; tiny deterministic instances; stochastic-arrival reproducibility; supplied non-learning baselines; minimal learning signal; focused studies of confirmed choices; then robustness, ablations, sensitivity, and generalization.

This order selects no specific value or method.

## Integrity rules

Do not invent or select scheduling constraints; arrival process; graph nodes, edges, features, or updates; MDP elements; algorithm or architecture; baselines; objectives; measures; instance distributions; scales; seeds; budgets; hardware; code identifiers; configuration keys; schemas; or paths.

## Output

Return confirmed facts with sources, unresolved or conflicting items, the smallest valid next experiment, the exact downstream Skill, and artifacts to save.
